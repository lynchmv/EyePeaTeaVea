REDIS_URL=redis://localhost:6379/0
HOST_URL=http://localhost:8020
# CORS_ALLOWED_ORIGINS defaults to "*" (allow all origins) which is recommended for Stremio addon compatibility.
# Stremio clients come from various origins and may not send a web-page origin.
# To restrict origins, set to a comma-separated list: CORS_ALLOWED_ORIGINS=http://example.com,https://example.com
# CORS_ALLOWED_ORIGINS=*
ADDON_ID=org.stremio.eyepeateavea
ADDON_VERSION=1.0.0
ADDON_NAME=EyePeaTeaVea
ADDON_DESCRIPTION="Stremio addon for M3U playlists"
ADDON_ID_PREFIX=eyepeateavea
# Optional: Clone tv-logos repository during Docker build to avoid GitHub rate limiting
# Set to "true" to clone the repository during build (recommended for production)
# Usage with docker build: docker build --build-arg CLONE_TV_LOGOS=true -t eyepeateavea .
# Usage with docker-compose: Set CLONE_TV_LOGOS=true in .env, then docker-compose build
# CLONE_TV_LOGOS=false

# Optional: Path to local tv-logos repository (set automatically by Dockerfile if CLONE_TV_LOGOS=true)
# Override this if you want to use a different path or mount a volume
# Example: TV_LOGOS_REPO_PATH=/app/tv-logos or ./tv-logos
# TV_LOGOS_REPO_PATH=

# Optional: Disable SSL verification for image fetching (not recommended for production)
# Set to "true" only if you encounter SSL certificate errors with certain CDNs
# DISABLE_SSL_VERIFY=false